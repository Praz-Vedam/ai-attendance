# Monorepo: deploy only the Next.js app in frontend/ (not backend/ or Python venvs).
[build]
  base = "frontend"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
